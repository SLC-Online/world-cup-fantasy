#!/usr/bin/env bash
# Convenience launcher: ./wcf <command> ...
# Uses the project virtualenv so you don't have to activate it.
set -euo pipefail
HERE="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
exec "$HERE/.venv/bin/python" -m wcf.cli "$@"
